import React, { useState } from "react";

const App = () => {
  const name = "John";

  return (
    <div className="App" style={{ padding: 30 }}>
      <h1>Hello, {name}</h1>
    </div>
  );
};

export default App;
